x = 3.14;  // Declares a global variable `x` and assigns it the value 3.14. 
           // This is allowed in non-strict mode, but it is considered bad practice 
           // because it implicitly creates a global variable without using `var`, `let`, or `const`.

console.log(x);  // Logs the value of `x` (3.14) to the console.
