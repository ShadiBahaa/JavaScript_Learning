// Initialize a variable `count` with a value of 0
let count = 0;

do {
    // Log the current value of `count` to the console
    console.log("Count is: " + count);
    // Increment the value of `count` by 1
    count++;
// Continue the loop as long as `count` is less than 5
} while (count < 5);