// Declare a variable `message` and assign it the string value 'Hello'
let message = 'Hello';

// Output the value of the `message` variable to the console
console.log(message);

// Reassign the `message` variable with a new string value 'World'
message = "World";

// Output the updated value of the `message` variable to the console
console.log(message);

// This is a single-line comment explaining that the following block demonstrates comments

/*
 * This is a multi-line comment.
 * It spans multiple lines and is used to provide detailed explanations or notes.
 */

// Output a string to the console to indicate that comments do not affect code execution
console.log("comments don't affect the code execution!");